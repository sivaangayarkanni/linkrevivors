{
  "name": "linkrevive",
  "version": "1.0.0",
  "private": true,
  "description": "LinkRevive - Dead Link Internet Fixer | Production Full-Stack + Extension",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:push": "cd apps/api && pnpm prisma db push",
    "db:migrate": "cd apps/api && pnpm prisma migrate deploy",
    "docker:up": "docker-compose up -d",
    "deploy:vercel": "cd apps/web && vercel --prod",
    "deploy:railway": "railway up"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
